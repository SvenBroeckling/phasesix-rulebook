[project]
name = "urpg-rulebook"
version = "0.1.0"
description = "The phase six rulebook"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "markdown>=3.8.2",
    "pyyaml>=6.0.2",
]

[dependency-groups]
dev = [
    "pre-commit>=4.3.0",
]
